base:
  - ./ai-sandbox.base.yaml

mounts:
  # mount shell configuration for a familiar shell experience
  - location: "~/.config/zsh"
    mountPoint: "{{ .Home }}/.config/zsh"
    writable: true
  # mount project directory for agent access
  - location: "~/projects"
    writable: true
